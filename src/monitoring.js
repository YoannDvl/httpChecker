"use strict";

var MonitoringConfig = require('./monitoringConfig.js').MonitoringConfig;
var Header = require('./monitoringConfig.js').Header;
var Post = require('./monitoringConfig.js').Post;

class Monitoring {
	static getConfigList(user) {

	}

	static saveConfig(config, user) {
	
	}

	static deleteConfig(config) {

	}

	static startMonitorings() {

	}

	static restartMonitoring(config) {

	}

	static stopMonitoring(config) {

	}
}
